#!/bin/sh

cd `dirname $0`

TARGET="../../src/include/version.h"
echo "/* File generated by genversion.sh, do not modify manually */"  >${TARGET}
echo "#define APEX_SVN_VERSION \""`svnversion ../../src/include`"\"" >> ${TARGET}
echo "#define APEX_VERSION \"`cat ../../VERSION`\""  >> ${TARGET}
echo "#define APEX_VERSION_MAJOR 3"  >> ${TARGET}
echo "#define APEX_VERSION_MINOR 0"  >> ${TARGET}
echo "#define APEX_VERSION_FLOAT 3.0"  >> ${TARGET}
echo "#define SVN_REVISION \"\$Rev$\""  >> ${TARGET}
echo "#define SCHEMA_VERSION \"`cat ../../VERSION_SCHEMA`\""  >> ${TARGET}
echo "#define EXPERIMENT_NAMESPACE \"http://med.kuleuven.be/exporl/apex/`cat ../../VERSION_SCHEMA`/experiment\""  >> ${TARGET}
